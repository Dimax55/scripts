----turn off KVM modul----------
sudo modprobe -r kvm_intel kvm
--------------------------------
sudo nginx -t
sudo systemctl restart nginx
sudo systemctl status nginx

------create SSH-Agent----------
ls ~/.ssh
ssh-keygen -t ed25519 -C "...@mail.com"
ls ~/.ssh
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
ssh -T git@github.com
---------------------------

--------rename server-----------
hostname   /    hostnamectl
sudo hostnamectl set-hostname new-server-name
hostnamectl
sudo nano /etc/hosts
(127.0.1.1   old-name)  ===>  (127.0.1.1   new-server-name)
sudo reboot
----------------------------

------Delete nginx----------
sudo systemctl stop nginx
sudo apt remove nginx nginx-common nginx-core
sudo apt purge nginx nginx-common nginx-core
sudo apt autoremove --purge
sudo apt autoclean
nginx -v
--------------------------
------SSH-KEYGEN----------
master-PC:
sudo systemctl restart ssh
sudo systemctl status ssh

ssh-keygen -b 4096
ll .ssh
cat .ssh/id_ed25519.pub
ssh-copy-id user@IPaddress
ssh user@IPadress
---------------------------
-----Change Users----------
su dima
-------------------------
Web-1
sudo apt update -y
sudo install nano -y
sudo install nginx -y
echo "
<!DOCTYPE html>
<html>
<head>
  <title>VM2</title>
</head>
<body>
  <h1>Bird from VM2</h1>
  <img src="data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQABAAD/2wCEAAkGBxISEBAQEhAPDxAPDw8>
</body>
</html>
" > index.html

Load Balancer
nano /etc/nginx/site-aval
-----------------------
---Static address-------
nano /etc/netplan/50-cloud-init.yaml

#network:
#  version: 2
#  ethernets:
#    enp0s3:
#      dhcp4: true
network:
  ethernets:
    enp0s3:
      addresses:
      - 192.168.0.54/24
      gateway4: 192.168.0.1
      nameservers:
        addresses:
        - 8.8.8.8
  version: 2

sudo netplan apply
-----------------------






