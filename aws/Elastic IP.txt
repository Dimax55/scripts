
Private IP - адрес доступній в середині мережі

Public IP - адрес доступній в інтернеті(тимчасовий, не змінується при reboot, змінюється при (вимк/вкл)start/stop instance)

Elastic IP -адрес доступній в інтернеті(на змінюється при вимк/вкл сервера)

